# Screenshots

Drop the following PNGs here (referenced by the main README's "Screens" section):

| Filename | Screen to capture |
|---|---|
| `home.png` | Home — phase card, supportive message, "Started period today" button |
| `checkin.png` | Today's Workout — the "First, how are you feeling today?" energy/symptom buttons |
| `recommendation.png` | Today's Workout — the recommendation + message + Sources, with the set logger and its "Previous" column |
| `history.png` | History — logged sessions, the weight-trend chart, and the period log |
| `phase-education.png` | Cycle & Learn — the cream phase-education boxes with the current phase highlighted |

Tip: capture at a comfortable desktop width; a light-mode capture reads best in the README
(optionally add a dark-mode shot as `home-dark.png`).
